<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd 
	http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">		

   <context:property-placeholder location="hadoop.properties"/>
				    
	<beans:bean id="sparkConfig" class="com.giantelectronicbrain.hadoop.spark.SparkConfig">
    		<beans:property name="applName" value="${spark.app.name}" />
    		<beans:property name="sparkHome" value="${spark.home}" />
    		<beans:property name="masterUri" value="${spark.master.uri}" />
	</beans:bean>
	
	<beans:bean id="wordCount" class="com.giantelectronicbrain.hadoop.spark.WordCount">
    		<beans:property name="inputFile" value="${spark.input.file}" />
    		<beans:property name="outputDir" value="${spark.output.dir}" />    		
    		<beans:property name="threshold" value="${spark.input.threshold}" />
    		<beans:property name="sparkConfig" ref="sparkConfig" />
	</beans:bean>

</beans:beans>